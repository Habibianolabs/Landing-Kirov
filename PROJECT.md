# PROJECT — G10 Киров

Дата актуализации: 19 августа 2026 года.

## Назначение

Статический лендинг образовательного проекта «G10. Рестораторы и опердиры России. Перезагрузка» для города Киров. Даты мероприятия в текущем контенте: 5–7 октября 2026 года.

## Стек и фреймворк

[FACT] Проект не использует Next.js, React, Vite или другой frontend-фреймворк и не имеет build step.

- HTML5: `index.html`;
- CSS: `styles.css`;
- vanilla JavaScript: `script.js`;
- Vercel Serverless Function: `api/submit-application.js`;
- Vercel deployment config: `vercel.json`;
- package metadata без runtime-зависимостей: `package.json`.

Vercel определяет проект как framework `null`; install/build/dev-команды не заданы. Статика раздаётся из корня репозитория, а `/api/*` обрабатывается функцией.

## Структура проекта

```text
/
├── index.html
├── styles.css
├── script.js
├── api/
│   └── submit-application.js
├── assets/
│   ├── site/kirov/              # подключённые production-assets
│   └── source-materials/        # канонический бриф и исходники
├── .env.example                 # только имена и безопасные примеры переменных
├── package.json
├── vercel.json
└── *.md                         # контекст, карта, план, handoff и TODO
```

`output/` и `.playwright-cli/` содержат локальные результаты визуальной проверки и не являются runtime-частью сайта. `.env.local` и `.vercel/.env.production.local` — локальные служебные файлы; секреты из них нельзя переносить в Git, Markdown или архив проекта.

## Production и репозиторий

- GitHub: [Habibianolabs/Landing-Kirov](https://github.com/Habibianolabs/Landing-Kirov);
- Git remote: `git@github.com:Habibianolabs/Landing-Kirov.git`;
- ветка: `main`;
- Vercel project: `g10-kirov`;
- прямой Vercel deployment с актуальной сборкой: [g10-kirov-habibiano.vercel.app](https://g10-kirov-habibiano.vercel.app/);
- публичный домен: [g10.kirov.restoved.ru](https://g10.kirov.restoved.ru/);
- Vercel alias [g10-kirov.vercel.app](https://g10-kirov.vercel.app/) перенаправляет на публичный домен.

[FACT] 14 августа 2026 года локальная версия опубликована в новом Vercel production deployment `dpl_AGSRivJHXqb7Sh6hoh36RFdYrZ5v` со статусом `READY`: [g10-kirov-habibiano.vercel.app](https://g10-kirov-habibiano.vercel.app/). В deployment присутствуют актуальные заголовки секций и шесть графических иллюстраций концепций.

[FACT] Публичный домен обслуживается отдельным сервером IHC: `95.181.224.17`, web-сервер `nginx/1.20.2`. Проверка 17 августа 2026 года: HTTP-запрос к этому IP с именем домена перенаправляет на `https://g10.kirov.restoved.ru/`, а HTTPS-ответ домена — `200 OK` от `nginx/1.20.2`.

[FACT] В корне сайта на сервере через защищённый FTP-доступ открыты `index.html`, папки `assets/` и `api/`, а также документация проекта. Доступ к списку файлов работает нестабильно: сервер иногда преждевременно закрывает защищённый канал передачи, но успешный список корня получен.

[UNKNOWN] URL панели управления IHC и способ входа в неё не подтверждены. Известен сайт провайдера `ihc.ru`, но это не является подтверждением адреса панели конкретного заказа. Учётные данные, пароли и содержимое `.env.local` в документации не фиксируются.

[FACT] Vercel Function `api/submit-application.js` не исполняется на IHC автоматически при простой FTP-загрузке: для формы на IHC нужна отдельная серверная настройка или проксирование в Vercel. До её подтверждения нельзя считать форму рабочего сайта на IHC активной.

[FACT] GitHub и локальная папка были синхронизированы 14 августа 2026 года в версии `fef0f7d`. Документация в этом обновлении изменяется локально и не публикуется на сервер до отдельной сверки файлов.

## Текущее состояние frontend

- светлый blue/white визуальный стиль;
- статичный баннер `assets/site/kirov/banner/g10-kirov-banner.jpg`, 1920×1080, без DOM-наложений, вращения и бликов;
- полупрозрачные blue-glass поверхности на всех основных секциях с fallback для браузеров без `backdrop-filter`;
- блок «О проекте» с пятью фотографиями: текущий кадр сохранён первым, переключение выполняется стрелками поверх фото с широкими кликабельными зонами, точками и свайпом;
- блок «Как проходят наши мероприятия» с тремя предоставленными фотографиями и lightbox; на desktop используется трёхколоночная сетка;
- секция «Партнёры проекта и их концепции»: у «Культуры Гостеприимства» шесть концепций, у «Маминой кухни» три;
- шесть карточек «Культуры Гостеприимства» используют графические иллюстрации из предоставленного референса в `assets/site/kirov/concepts/illustrations/`; названия, характеристики и раскрывающиеся описания остаются живым HTML-контентом. Три карточки «Маминой кухни» сохраняют фотографии, так как графические варианты для них в референсе не были предоставлены;
- секция «Партнёры программы» возвращена к исходной композиции: стандартный фон секции, обычные внешние карточки и одинаковые внутренние рамки вокруг обоих логотипов с фоном `var(--surface-muted)`; названия секций оформлены увеличенными центрированными liquid-glass web-плашками;
- в блоке о компании подпись-кicker полностью указана как «Ресторанный альянс «Культура гостеприимства»» и переносится внутри рамки без переполнения;
- sticky header, мобильное меню, якорная навигация, dialogs, disclosures и lightbox;
- блок «Эксперты» сохраняет название блока, но удалён заголовок «Люди, которые строят систему изнутри»;
- начиная с блока «Эксперты» крупные заголовки секций до CTA-блока удалены; названия блоков сохранены, CTA-заголовок «Станьте самым эффективным операционистом собственного бизнеса» оставлен;
- блок «Ключевые темы проекта» содержит 14 пунктов с нумерацией `1`–`4` внутри четырёх групп;
- обе формы имеют client-side validation, consent и honeypot-поле;
- consent banner и отложенная загрузка аналитики реализованы; Яндекс Метрика подключена локально и ожидает публикации на IHC.

## Backend формы

`api/submit-application.js` уже содержит:

- POST/OPTIONS endpoint;
- server-side нормализацию и валидацию;
- honeypot `website`;
- rate limiting через Upstash Redis в production и memory fallback только локально;
- optional Cloudflare Turnstile server verification;
- отправку через Resend с `Reply-To` заявителя;
- обработку loading/success/error на клиенте.

[FACT] Production-доставка не активна до добавления в Vercel Environment Variables реальных `RESEND_API_KEY`, подтверждённого `RESEND_FROM_EMAIL`, `UPSTASH_REDIS_REST_URL` и `UPSTASH_REDIS_REST_TOKEN`. Четыре адреса в `.env.example` являются текущим fallback из материалов проекта и не заменяют подтверждение финального адреса заказчиком.

## Что осталось сделать

Обязательное для рабочего запуска:

1. Подтвердить финальный адрес/адреса получателей и отправителя.
2. Создать Resend API key и подтвердить домен или адрес отправителя.
3. Создать Upstash Redis и добавить production-переменные в Vercel.
4. Выполнить production deploy и безопасный smoke test формы с согласованным тестовым адресом.
5. Подключить домен заказчика и проверить DNS/SSL.
6. Закрыть финальную content/accessibility/performance-приёмку.

Опционально после запуска: Turnstile widget, Envybox/CRM/Telegram.

[FACT] 18 августа 2026 года в аккаунте заказчика создан счётчик Яндекс Метрики `G10 Kirov` для `g10.kirov.restoved.ru`, номер `111727875`. В `script.js` он загружается только после согласия посетителя на аналитические cookies. Без передачи имени, телефона или e-mail фиксируются обычные просмотры и время на сайте, открытия формы, выбранный тариф, клики по телефонам и сайтам партнёров, глубина прокрутки 50/90% и только успешная отправка заявки.

[UNKNOWN] Код Метрики пока не опубликован на IHC, поэтому данные с публичного сайта ещё не собираются.

Открытые контентные решения: финальная программа, лимит группы 20/25, ценовой период 29 августа–4 сентября, 14/13 тем, портрет Михаила Скрябина для третьей большой карточки, финальные отзывы и права на фотографии мероприятий и карусели «О проекте»/Rutube.

## Источники и ограничения

Канонический источник — бриф `assets/source-materials/Бриф для лендинга Киров.docx`, затем актуальная папка `/Users/lilcheecha/Downloads/G10 Рестораторы России Киров 3`. Екатеринбург используется только как небольшой функциональный референс расположения блоков и поведения. Публичный проект — только Киров; неподтверждённые данные, черновое расписание с `???` и случайные фотографии нельзя выдавать за финальные.
