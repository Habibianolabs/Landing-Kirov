# Backend handoff — G10 Киров

Дата актуализации: 28 августа 2026 года.

## Актуальная схема с 26 августа

[FACT] Vercel исключён из системы заявок. Браузер отправляет JSON только на `/api/submit-application.php`. После недоставки через `mail()` новая версия PHP переведена на авторизованный SMTP Яндекс 360 через PHPMailer. Отправитель — `event@restoranoff.ru`; четыре адреса заказчика получают отдельные письма. Пароль приложения хранится только в закрытой переменной сервера. `api/submit-application.js`, `vercel.json` и `.vercelignore` удалены.

[FACT] Полученное пользователем 26 августа письмо было исторической проверкой через внешний сервис и не доказывает production-схему. Этот сервис и его ключ полностью удалены. В production получатели зафиксированы в PHP; заказчик ничего не настраивает. Сисадмин только загружает файлы и сообщает о готовности к одной финальной проверке. Полная инструкция — [`FORM_DELIVERY.md`](FORM_DELIVERY.md).

[FACT] Resend и его локальный ключ удалены. 28 августа локальный лендинг отправил реальную заявку через Яндекс SMTP, и письмо получено в Gmail. Личный Gmail после теста удалён из production-получателей. Production станет работоспособным после установки `G10_SMTP_PASSWORD`, загрузки `api/lib/phpmailer` и реальной проверки четырёх ящиков. Инструкция — [`SMTP_SETUP.md`](SMTP_SETUP.md).

Этот файл — короткая рабочая инструкция для нового чата `backend`. Он описывает фактическое состояние проекта на момент передачи. Если другой Markdown-файл говорит иначе, сначала сверить дату и считать этот файл оперативной сводкой, а `PROJECT_STATUS.md` — таблицей готовности функций.

## 1. Границы проекта

[FACT] Делаем только лендинг G10 «Рестораторы и опердиры России. Перезагрузка» для Кирова. Екатеринбург — только функциональный референс; его город, тексты, дизайн и случайные виджеты не переносим.

[FACT] Проект статический: `index.html`, `styles.css`, `script.js`, локальные assets и PHP-обработчик. React, Next.js, Vite и сборщик не используются.

[FACT] Production — сервер заказчика IHC и домен `https://g10.kirov.restoved.ru/`. Vercel `https://g10-kirov.vercel.app/` нужен только для показа frontend и не доказывает работу PHP или почты.

[FACT] Файлы на IHC загружает сисадмин. В этом handoff нет паролей, токенов, приватных ключей или содержимого `.env.local`.

## 2. Где находится код и что опубликовано

- Рабочая папка: `/Users/lilcheecha/Documents/Landings`.
- GitHub: `https://github.com/Habibianolabs/Landing-Kirov`, репозиторий открыт, ветка `main`.
- Последний общий runtime-коммит локальной ветки и `origin/main`: `97a43ce` (`content: split partner showcase sections`).
- [FACT] Ветка `main` и `origin/main` остаются на коммите `97a43ce`. В рабочей папке есть незакоммиченные изменения `index.html`, `styles.css` и новый `LANDING_DEVELOPMENT_GUIDE.md`; в этот backend-тест они не включались. IHC не изменялся.
- [FACT] `api/submit-application.php` не менялся в этой серии правок. Vercel-деплой `dpl_65z5q9bCz1MeURrPnPeL2tGF7BaQ` получил статус `READY`, alias `https://g10-kirov.vercel.app` обновлён.
- [FACT] Предыдущая сверка с IHC подтверждала старую runtime-версию. Последние версии backend/frontend нужно загрузить на IHC и затем проверить отдельно.

Не загружать на сервер и не коммитить: `.env.local`, `.vercel/.env.production.local`, `.git`, `webstat` и секреты.

## 3. Backend формы: что сделано и что сломалось

Production-маршрут формы: `/api/submit-application.php`.

[FACT] В PHP уже есть:

- проверка метода, размера запроса, источника и обязательных полей;
- нормализация имени, телефона и e-mail;
- согласие на обработку данных;
- скрытое поле-ловушка `website` против простого спама;
- проверка минимального времени заполнения;
- ограничение частоты по IP и защита от повторной заявки;
- отправка через встроенную почтовую функцию хостинга `mail()` с `Reply-To` заявителя;
- понятные ответы для frontend: ошибки, успех и техническая недоступность.

[FACT] На IHC проверено: `OPTIONS /api/submit-application.php` отвечает `204`, неполная заявка отвечает `400`, PHP исполняется как версия 7.3.33.

[FACT] 21 августа 2026 года одна заявка с пометкой «ТЕХНИЧЕСКАЯ ПРОВЕРКА НЕ ЗАЯВКА» получила от публичного IHC-обработчика `200 OK`. Предыдущая ошибка rate-limit `503` больше не воспроизводится. Так как обработчик возвращает успех только после четырёх успешных вызовов `mail()`, локальная почтовая система IHC приняла отправку каждому из четырёх получателей.

[FACT] В PHP зафиксированы получатели: `lp@restoranoff.ru`, `rv@restoranoff.ru`, `event@restoranoff.ru`, `p.spiridonova@restoranoff.ru`. Адрес отправителя: `event@restoranoff.ru`.

[UNKNOWN] Фактическое появление тестового письма во входящих или спаме каждого адресата не подтверждено: четыре целевых ящика и журналы исходящей почты IHC недоступны. Ответ `200` подтверждает принятие отправки, но не окончательную доставку.

[FACT] У `restoranoff.ru` опубликованы две SPF-записи вместо одной. Одна из них разрешает IP IHC `95.181.224.17`, но дублирование SPF является ошибкой DNS и может ухудшать доставляемость. MX домена направлен на Яндекс 360, DKIM-запись присутствует, DMARC использует режим `quarantine`. Исправление SPF должен выполнить администратор DNS/почты заказчика.

## 4. Метрика и Директ

[FACT] Счётчик Яндекс Метрики `G10 Kirov`, номер `111727875`, подключается в `script.js` только после согласия посетителя на аналитические cookies. Он может собирать просмотры, длительность, глубину прокрутки, открытие формы, выбор тарифа, клики по телефонам и партнёрам и успешную заявку. Имя, телефон и e-mail в события не передаются.

[FACT] В аккаунте Яндекс Директа `e-16563934` сохранён источник `G10 Kirov` и автоматическая цель «заполнил контактные данные».

[FACT] 21 августа 2026 года на публичном IHC-домене браузер подтвердил загрузку счётчика после согласия на аналитические cookies, отправку просмотра страницы и отдельного события `application_open` при открытии формы.

[FACT] Пользовательские JavaScript-цели с именами `application_open`, `tariff_selected`, `phone_click`, `partner_site_open`, `application_submitted` ещё не созданы в интерфейсе Метрики: авторизованный кабинет продолжает возвращать `503` и показывает сообщение Яндекса «Мы уже в курсе и скоро всё починим». Это внешний блокер на стороне Яндекса, а не ошибка кода лендинга.

## 5. EnvyBox

[FACT] Код EnvyBox добавлен в `script.js` и загружается после полного согласия cookies:

```html
<link rel="stylesheet" href="https://cdn.envybox.io/widget/cbk.css">
<script type="text/javascript" src="https://cdn.envybox.io/widget/cbk.js?wcb_code=28c890d5bde2398ab07faf542b1c5b46" charset="UTF-8" async></script>
```

[FACT] После замены кода локальная и публичная Vercel-браузерная проверка с полным согласием cookies показала чат «Мы онлайн, задавайте вопросы!» и кнопку «Закажите звонок!». Это подтверждает работу нового кода для локального сайта и Vercel-frontend. Новую версию `script.js` ещё нужно загрузить на IHC и проверить на production-домене.

## 5a. Скорость frontend

[FACT] Главный баннер подключён через отдельный файл 1600×900 размером 484 КБ; исходный 1920×1080 оставлен fallback без изменений. Скрытые изображения карусели и галереи загружаются по приближению/действию, Rutube — после прокрутки. Локальная проверка уменьшила начальный сетевой набор примерно с 10 МБ до 733 КБ; публичный Vercel-браузер подтвердил выбранный баннер, отсутствие начального Rutube-запроса и отсутствие горизонтального переполнения.

[FACT] IHC в рамках этой работы не изменялся.

## 6. Известные ошибки и ограничения

1. Production rate-limit больше не блокирует заявку: тест 21 августа вернул `200`. Остаётся подтвердить четыре целевых ящика и исправить двойную SPF-запись домена.
2. Интерфейс Яндекс Метрики временно отвечал `503`, поэтому пользовательские цели не заведены.
3. IHC пока содержит предыдущую runtime-версию; отображение EnvyBox на production после загрузки нового `script.js` ещё не проверено.
4. [FACT] 22 августа через сохранённую запись FileZilla `g10-kirov` выполнены повторные попытки подключения к `95.181.224.17:21`. И защищённый режим, и временно разрешённый пользователем обычный FTP установили соединение с адресом, но не получили приветствие FTP и завершились по тайм-ауту. Удалённая папка не открылась, серверный файл не скачивался и не загружался. Настройка FileZilla восстановлена на `Use explicit FTP over TLS if available`. Это блокер FTP-службы/соединения на стороне хостинга или сетевого доступа, а не подтверждённая ошибка пароля.
5. Production PHP 21 августа принял тестовую отправку на четыре адреса; получение каждым ящиком и двойная SPF-запись ещё требуют проверки почтового администратора.

## 7. Что делаем сейчас

[FACT] Текущий шаг — устранить блокер FTP-доступа к IHC, после чего сохранить серверный PHP-файл, временно добавить пятый тестовый адрес и проверить доставку через текущий домен.

[FACT] 21 августа отправлена одна явно помеченная техническая заявка. IHC вернул `200`, то есть принял четыре вызова `mail()`; фактическое получение целевыми ящиками ещё не подтверждено.

[FACT] Непосредственный рабочий блок после передачи — передать сисадмину новую версию `script.js` вместе с подтверждённым набором runtime-файлов, проверить PHP-почту и отдельно повторить проверку EnvyBox на IHC.

[FACT] Предыдущая временная проверка с личной почтой пользователя не была выполнена: серверный PHP-файл не изменялся, личный адрес в него не добавлялся, тестовая заявка через домен после этого не отправлялась. Причина — FTP-служба не открыла удалённую папку.

## 8. Что делать в новом backend-чате

1. Сначала прочитать `AGENTS.md`, `NEW_CHAT_CONTEXT.md`, этот файл, `PROJECT_STATUS.md`, `CURRENT_TASK.md`, `HANDOFF.md`, `TODO.md` и `MISSING_MATERIALS.md`.
2. Не менять сервер и не отправлять тестовую заявку без отдельного согласования пользователя. Не использовать Vercel как backend.
3. Сверить локальные `script.js` и `api/submit-application.php` с GitHub `main`; если backend-файлы не менялись, передавать сисадмину именно версии из GitHub.
4. Отдельно решить, должны ли локальные изменения `index.html`, `styles.css` и `mamina-kuhnya-attached.jpg` войти в релиз. Только после этого их можно коммитить и передавать сисадмину.
5. Сисадмин загружает подтверждённые runtime-файлы на IHC маленькими порциями, не трогая `.env.local`, `.git` и `webstat`.
6. Техническая заявка уже отправлена. Проверить точную тестовую тему во входящих и спаме всех четырёх адресов либо в журнале Яндекс 360/IHC; объединить две SPF-записи домена в одну.
7. После восстановления интерфейса Метрики создать цели для событий и выбрать `application_submitted` основной целью в Директе.
8. Загрузить новый `script.js` на IHC; затем проверить чат и кнопку обратного звонка на desktop и mobile.
9. Завершить адаптивность, доступность, скорость, права на media и финальные контентные решения.

## 9. Правила безопасности

- Не просить пользователя присылать пароль, токен, приватный ключ или `.env.local`.
- Не публиковать секреты в GitHub, HTML, JavaScript, PHP или Markdown.
- Не удалять и не перезаписывать файлы на IHC без точного списка и проверки.
- Любой технический термин объяснять простыми словами и отдельно писать, что делает Codex, что делает пользователь/сисадмин и какой результат ожидается.
