# План реализации лендинга G10 Киров

Дата подготовки: 24 июля 2026 года. Текущая сверка: 20 августа 2026 года.

Короткий актуальный статус по функциям: [`PROJECT_STATUS.md`](PROJECT_STATUS.md). Оперативная передача backend-задач: [`BACKEND_CHAT_CONTEXT.md`](BACKEND_CHAT_CONTEXT.md).

## Текущий статус реализации

Дата статуса: 20 августа 2026 года.

[FACT] Frontend-части этапов 2–11 и 14 закрыты в виде статического HTML/CSS/JavaScript проекта. Демонстрационный frontend доступен в Vercel deployment `g10-kirov`; публичный домен `g10.kirov.restoved.ru` обслуживается отдельным IHC origin `95.181.224.17` с `nginx/1.20.2`.

[PARTIAL] Доступ к файловому корню IHC получен через защищённый FTP: видны `index.html`, `assets/`, `api/` и документация. Предыдущая runtime-версия подтверждённо совпала с IHC; текущие локальные frontend-изменения и backend-версию нужно загрузить отдельными проверяемыми порциями. `.env.local`, `.git` и `webstat` должны быть исключены из публикации.

[PARTIAL] PHP-маршрут `api/submit-application.php` создан для IHC, опубликован в GitHub и подтверждён на публичном домене (`OPTIONS` — `204`, PHP 7.3.33). Валидная техническая заявка получила `503` до отправки письма из-за rate limit storage; после загрузки подтверждённой версии нужна повторная проверка. Адрес панели управления конкретного заказа остаётся неизвестным.

[FACT] Реализованы header/navigation, hero, вводный quote-блок, партнёры программы, содержание программы, «О проекте», партнёры проекта и dialogs, placeholder программы, эксперты, статичные темы `1`–`4`, стоимость, состав участия, галерея/lightbox, отзывы, cookie consent, footer и формы. Основные названия секций оформлены едиными центрированными liquid-glass web-плашками, включая верхнюю плашку «О проекте», с единым отступом до содержимого 40px на desktop и 28px на mobile. В секции о компании подпись-кicker — «Ресторанный альянс «Культура гостеприимства»» — переносится внутри рамки.

[FACT] Шесть концепций «Культуры Гостеприимства» и три концепции «Маминой кухни» используют отдельные графические иллюстрации из предоставленного референса; данные карточек остаются живым HTML-контентом.

[FACT] В секциях от «Экспертов» до CTA сохранены section-kicker-названия блоков, а крупные H2 удалены по дизайн-решению; CTA-заголовок оставлен.

[PARTIAL] Открыты content freeze, подтверждение PHP/mail на IHC, права на media, accessibility/performance QA, production analytics verification, Turnstile, EnvyBox production check и финальный release checklist. [FACT] Счётчик Яндекс Метрики `G10 Kirov` № `111727875` опубликован на IHC в предыдущей синхронизации; после загрузки подтверждённой текущей версии требуется повторная проверка событий. Новый EnvyBox-код локально и в Vercel показал чат и кнопку обратного звонка после consent; IHC ещё не обновлён.

[PARTIAL] PHP-обработчик формы создан и опубликован в GitHub: validation, honeypot, проверка времени, файловое rate limiting, защита от повторов, проверка источника и PHP `mail()` готовы. PHP уже исполняется на IHC; после загрузки подтверждённой версии реальная доставка письма всё ещё требует проверки.

Ниже сохранён исходный поэтапный план как журнал решений. Авторитетный список текущих действий находится в `TODO.md`, а точные блокеры — в `MISSING_MATERIALS.md`.

## 1. Цель

Создать современный адаптивный лендинг мероприятия «G10. Рестораторы и опердиры России. Перезагрузка» в Кирове, сохранив функциональную полноту лендинга Екатеринбурга, но используя дизайн утвержденного эскиза и палитру готового баннера.

[FACT] Этот документ первоначально описывал план до начала кодирования; актуальное состояние и незакрытые задачи ведутся в `CURRENT_TASK.md`.

[FACT] Техническая основа статическая: семантический HTML, CSS и vanilla JavaScript. React, Next.js, Vite и сборщик не используются; серверная часть production — PHP endpoint на IHC.

## 2. Исходное состояние репозитория

[FACT] В корне находится статический frontend-лендинг G10 Киров: `index.html`, `styles.css`, `script.js`, `assets/site/kirov`, `vercel.json`.

[FACT] Ветка — `main`; последний общий runtime-коммит локальной ветки и GitHub — `97a43ce`. Рабочее дерево чистое, текущий frontend опубликован в Vercel, а исторический baseline `9ddc7b3` оставлен только как точка начала проекта.

[INFERENCE] Существующий код можно использовать как технический ориентир для семантики, модальных окон и доступных аккордеонов, но публичный контент, айдентика, партнеры, портреты и изображения должны соответствовать Кировскому брифу.

### Обязательное действие до разработки

1. Зафиксировать актуальное состояние Кирова и проверить источники.
2. Считать Киров единственным публичным маршрутом и корнем проекта.
3. Создать отдельную ветку с префиксом `codex/`, если пользователь подтвердит работу в этом репозитории.
4. Удалять только проверенные устаревшие или дублирующиеся assets после аудита ссылок и изображений.

## 3. Архитектурные принципы

- Один источник данных для повторяющихся сущностей: партнеры, рестораторы, темы, тарифы, отзывы и получатели формы.
- Один экземпляр модального окна заявки для всех CTA.
- Один универсальный механизм disclosure для программы и partner details; темы сейчас статично раскрыты.
- Один универсальный доступный dialog-controller для заявки, партнеров и lightbox.
- Контент остается читаемым без JavaScript; JavaScript добавляет раскрытие, модальные окна и аналитику.
- Баннер — атомарный asset. На него не накладываются DOM-элементы.
- Все внешние изображения и видео получают локальную/контролируемую стратегию хранения; production не зависит от Tilda CDN для статичных изображений.
- Аналитика и маркетинговые виджеты загружаются только после соответствующего cookie consent.

## 3.1. Что берем из лендинга Екатеринбурга

[FACT] В референсе проверены следующие рабочие паттерны:

- desktop-header: пять якорей и отдельный CTA;
- mobile-header: burger, вертикальное белое меню и отдельный CTA;
- последовательность: hero → вводный quote-блок → партнерские подписи → содержание программы → о проекте → партнеры → программа → CTA → рестораторы → темы → CTA → стоимость → состав стоимости → медиа → отзывы → форма → footer;
- партнерские карточки с кнопкой «Подробнее» и dialog с длинным описанием;
- темы как четыре статично раскрытые группы; все пункты видны без клика;
- программа референса сейчас показывает placeholder «Скоро будет добавлена»;
- два тарифа, четыре пункта состава цены;
- три zoomable event-фото и широкое Rutube-видео;
- пять текстовых отзывов и один видеоотзыв;
- встроенная форма и отдельная форма в modal;
- cookie banner с Accept/Reject/Settings и четырьмя категориями;
- responsive viewport 393 px без горизонтального overflow.

**Не копируем:**

- цвета, типографику, декоративные изображения и композицию Екатеринбурга;
- Tilda zero-block geometry и абсолютное позиционирование;
- предварительно отмеченное согласие формы;
- необязательные поля без `required`/серверной проверки;
- hotlink статичных Tilda assets;
- mobile-конфликт, при котором чат перекрывает hero и открытое меню;
- скрытые popup-заготовки, у которых нет работающих triggers.

## 4. Фактическая структура файлов

Текущая структура runtime и ключевых материалов:

- `index.html` — семантические секции, формы, dialog-контейнеры, footer;
- `styles.css` — tokens, layout, компоненты, responsive, print/reduced motion;
- `script.js` — навигация, disclosure, dialogs, lightbox, формы, consent, analytics events;
- `assets/source-materials/` — канонический бриф;
- `assets/site/kirov/banner/` — готовый JPEG banner;
- `assets/site/kirov/logos/` — логотипы;
- `assets/site/kirov/photos/about/` — три фотографии карусели «О проекте» из предоставленной папки;
- `assets/site/kirov/events/` — три подключённых event-фото и архивный набор предыдущих кадров;
- `api/submit-application.php` — production PHP endpoint формы на IHC;
- `FORM_DELIVERY.md` — актуальная инструкция по production-почте без внешних секретов;
- `PROJECT.md`, `AGENTS.md` и остальные Markdown в корне — актуальный контекст, план и handoff;
- `output/` и `.playwright-cli/` — локальные QA-артефакты, не runtime.

## 5. Компоненты

Под «компонентами» здесь понимаются самостоятельные логические UI-блоки; React-компоненты не требуются.

| Компонент | Ответственность | Данные |
|---|---|---|
| `SiteHeader` | sticky-навигация, active section, desktop/mobile menu | якоря, CTA |
| `HeroBanner` | неизмененный баннер 16:9 и отдельный CTA | `g10-kirov-banner.jpg` |
| `ProgramPartnersStrip` | два логотипа с подписями | логотипы и подписи из брифа |
| `ProgramBenefits` | шесть пунктов содержания программы | тексты Екатеринбурга по брифу |
| `AboutProject` | общий текст и фотокарусель | текст референса, три фотографии «О проекте» |
| `ProjectPartners` | две крупные карточки | данные альянса и «Маминой кухни» |
| `PartnerDialog` | подробное описание выбранного партнера | структурированные тексты брифа |
| `ProgramSchedule` | placeholder, позже три дня-аккордеона | утвержденное расписание |
| `InlineCta` | повторяемый оффер и кнопка | оффер блока |
| `RestaurateursGrid` | три карточки рестораторов | имена, должности, портреты |
| `TopicsList` | четыре статично раскрытые группы | 14 тем из брифа; номера `1`–`4` |
| `PricingGrid` | два тарифа и периоды | цены брифа |
| `IncludedGrid` | четыре пункта состава цены | тексты референса |
| `EventGallery` | три фото, lightbox и видео | локальные event assets, Rutube ID |
| `TestimonialsGrid` | компактные текстовые и видеоотзыв | отзывы референса |
| `ApplicationForm` | inline и modal presentation одной схемы | имя, телефон, email, consent, metadata |
| `ApplicationDialog` | доступное модальное окно формы | общий form schema |
| `CookieConsent` | первый уровень и настройки категорий | политика, четыре категории |
| `SiteFooter` | контакты, документы, повторная навигация | телефоны, email, ссылки |
| `AnalyticsAdapter` | единый слой событий без vendor lock-in | карта событий и ID счетчиков |
| `ChatAdapter` | отложенная загрузка Envybox | ключ/конфигурация Envybox |

## 6. Этапы разработки

### Этап 0. Закрытие блокеров и content freeze

**Что делается:**

- получить ответы по `MISSING_MATERIALS.md`;
- определить финальный лимит группы: 20 или 25;
- подтвердить набор и очередность ценовых периодов;
- подтвердить, публикуется ли только placeholder программы;
- получить недостающие логотипы, портреты и фото «Маминой кухни»;
- получить Envybox-конфигурацию, опубликовать и проверить созданный счётчик Метрики, подтвердить PHP/mail-канал IHC;
- зафиксировать, какие удаленные assets Екатеринбурга разрешено перенести локально.

**Компоненты:** не создаются.

**Данные:** `CONTENT_MAP.md`, `MATERIALS_INDEX.md`, `MISSING_MATERIALS.md`.

**Результат:** таблица решений без `[UNKNOWN]`, подписанный список production-assets.

### Этап 1. Безопасная подготовка репозитория

**Что делается:**

- сохранить актуальное состояние Кирова;
- считать корень проекта единственным маршрутом Кирова;
- проверить `.gitignore`, чтобы QA-артефакты не попадали в commit;
- перенести исходные материалы в `assets/source-materials/` без переименования и конвертации;
- сформировать manifest asset → блок → статус использования.

**Компоненты:** не создаются.

**Данные:** все локальные материалы.

**Результат:** воспроизводимая исходная база и отсутствие риска потерять предыдущий лендинг.

### Этап 2. Подготовка production-assets

**Что делается:**

1. Открыть PSD в Photoshop или совместимом приложении, которое корректно поддерживает слои.
2. Экспортировать единый PNG 1920×1080 без редактирования.
3. Сравнить экспорт с утвержденным эскизом и контрольным изображением: текст, положение элементов, цвета, края.
4. Не применять crop, retouch, overlays, новый текст, смену шрифтов или цветокоррекцию.
5. Выбрать реальные фотографии партнеров и локаций с участием заказчика.
6. Создать web-derivatives фотографий: корректная ориентация, безопасный crop, responsive размеры, WebP/AVIF только для фотографий.
7. Не конвертировать баннер в lossy-формат без отдельного разрешения.
8. Добавить `alt`-описания и реестр авторских/лицензионных источников.

**Компоненты:** asset manifest.

**Данные:** PSD, 37 JPEG, четыре event-изображения референса, портреты/логотипы после получения.

**Результат:** полный набор оптимизированных production-assets без изменения баннера.

### Этап 3. Design tokens и фон

**Статус на 6 августа 2026:** реализованы светлая blue/white палитра, статичный голубой фон с малоконтрастным pattern и blue-glass surfaces на основных секциях. В карточках партнёров программы поверхности логотипов настроены отдельно для каждой компании.

**Что делается:**

- извлечь palette samples из корректного баннера;
- определить CSS-токены: фон, поверхности, текст, muted text, primary blue, dark blue, border, success/error, shadows, radii, spacing, typography;
- построить светлый голубой фон без резких пятен;
- создать едва заметный диагональный pattern;
- проверить контраст текста на всех поверхностях;
- ограничить декоративный pattern по opacity и density.

**Компоненты:** `PageBackground`, design tokens.

**Данные:** баннер, эскиз.

**Результат:** визуальная база, которая поддерживает баннер, но не копирует Екатеринбург.

### Этап 4. Каркас страницы и навигация

**Что делается:**

- семантический порядок `header → main → sections → footer`;
- skip-link к основному содержимому;
- sticky header с якорями;
- корректный `scroll-margin-top`;
- active-section через `IntersectionObserver` с fallback;
- mobile menu как доступная панель;
- закрытие меню после перехода;
- отсутствие горизонтального overflow на 320–393 px.

**Компоненты:** `SiteHeader`, `MobileNavigation`.

**Данные:** карта навигации из эскиза.

**Результат:** пустой каркас со всеми якорями и рабочей адаптивной навигацией.

### Этап 5. Hero и верхние информационные блоки

**Порядок:**

1. `HeroBanner`.
2. `ProgramPartnersStrip`.
3. `ProgramBenefits`.
4. `AboutProject`.

**Что делается:**

- баннер отображается целиком 16:9;
- CTA вынесен из изображения;
- логотипы партнеров не заменяются текстовыми имитациями;
- шесть program benefits получают единый ритм и иконки;
- about desktop строится 2 колонки, mobile — 1 колонка.

**Компоненты:** `HeroBanner`, `ProgramPartnersStrip`, `ProgramBenefits`, `AboutProject`.

**Данные:** баннер, логотипы, шесть текстов, подтвержденный about-текст, три фотографии «О проекте».

**Результат:** завершенная верхняя половина лендинга.

### Этап 6. Партнеры проекта и модальные окна

**Что делается:**

- две визуально равнозначные партнерские карточки;
- текстовые факты остаются HTML, не вшиваются в изображения;
- общий dialog-controller;
- контент партнеров хранится отдельно от DOM-шаблона;
- длинный текст структурируется подзаголовками;
- focus trap, Escape, overlay click, restore focus;
- на mobile окно занимает доступную высоту и прокручивается внутри.

**Компоненты:** `ProjectPartners`, `PartnerCard`, `PartnerDialog`.

**Данные:** тексты брифа; фото альянса; фото «Маминой кухни»; логотипы, если решено показывать их в карточках.

**Результат:** партнерские описания доступны без перегрузки основной страницы.

### Этап 7. Программа и повторный CTA

**Что делается:**

- сначала реализуется согласованный placeholder;
- DOM сразу проектируется под три дня;
- draft 5–6 октября не попадает в production;
- после утверждения данные вставляются без перестройки компонента;
- каждая запись программы имеет время, локацию, тип, тему и ведущего;
- альтернативные варианты/внутренние комментарии отфильтровываются.

**Компоненты:** `ProgramSchedule`, `DayDisclosure`, `ScheduleItem`, `InlineCta`.

**Данные:** оффер брифа; позднее — утвержденная таблица трех дней.

**Результат:** честное состояние «скоро» сейчас и готовность к добавлению расписания позже.

### Этап 8. Рестораторы

[FACT] Отдельная секция «Эксперты» удалена вместе с пунктами верхней и нижней навигации. Список из 10 экспертов альянса остаётся внутри карточки «Культура Гостеприимства»: две колонки на desktop, одна на mobile, без раскрытия.

**Данные:** локальные миниатюры портретов, имена и должности экспертов альянса. Отдельные большие карточки и биографические модальные окна не используются.

**Результат:** в интерфейсе не остаётся самостоятельного блока «Эксперты» или ссылок на удалённую секцию.

### Этап 9. Ключевые темы

**Статус на 6 августа 2026:** четыре группы реализованы статично раскрытыми; все 14 пунктов видны сразу, а номера групп — `1`, `2`, `3`, `4`.

**Что делается:**

- четыре статичные группы без disclosure-контролов;
- текст mobile не мельче основного.

**Компоненты:** `TopicsList`, `TopicGroup`, `InlineCta`.

**Данные:** 14 тем и оффер из брифа; вопрос объединения до 13 закрывается до content freeze.

**Результат:** читаемая интерактивная секция без мелкого текста.

### Этап 10. Стоимость и состав участия

**Что делается:**

- две тарифные карточки;
- текущий первый период выделен;
- следующие периоды показаны вторично после решения о календарном gap;
- CTA передает тариф в форму;
- четыре пункта «Что входит» формируют отдельную сетку;
- на mobile карточки идут последовательно, цена не разрывается с названием тарифа.

**Компоненты:** `PricingGrid`, `PriceCard`, `PriceTimeline`, `IncludedGrid`.

**Данные:** таблица цен брифа, четыре пункта референса.

**Результат:** коммерческий блок с однозначными ценами и CTA.

### Этап 11. Медиа и отзывы

**Что делается:**

- три предоставленные event-фото с lazy loading;
- доступный lightbox с клавиатурой;
- Rutube iframe остаётся видимым в блоке без autoplay, но его загрузка отложена до прокрутки к видеоблоку;
- review cards ограниченной ширины;
- длинный отзыв Александры Цыбульской остаётся за пределами текущей опубликованной версии до отдельного решения;
- внешняя ссылка на видеоотзыв имеет понятное описание;
- iframe появляется по мере прокрутки по требованию заказчика; отдельные vendor scripts кроме самого Rutube embed не добавляются.

**Компоненты:** `EventGallery`, `Lightbox`, `VideoEmbed`, `TestimonialsGrid`, `TestimonialCard`.

**Данные:** три предоставленные event-фото, Rutube ID, четыре опубликованные текстовые карточки и отдельный видеоотзыв.

**Результат:** социальное доказательство без широких тяжелых плашек Екатеринбурга.

### Этап 12. Формы и серверная доставка

**Статус на 20 августа 2026:** PHP endpoint исполняется на IHC, но валидный технический запрос был остановлен до почтовой функции ошибкой хранилища rate limit. После загрузки подтверждённой версии остаётся проверка доставки.

**Что делается:**

1. Единая form schema для inline и modal формы.
2. Поля: имя, телефон, email, consent.
3. Скрытые данные: тариф и CTA source.
4. Клиентская валидация и accessible errors.
5. Серверная проверка и нормализация.
6. Отправка на четыре адреса через защищенный server-side transport.
7. Rate limiting, honeypot и optional Turnstile-серверная проверка.
8. Безопасные ответы API без раскрытия технических секретов.
9. Success/error UI и аналитические события.
10. Ни API-ключи, ни SMTP credentials, ни recipient routing не размещаются в клиентском JavaScript.

**Компоненты:** `ApplicationForm`, `ApplicationDialog`, `FormController`, server-side `LeadEndpoint`.

**Данные:** четыре email, тексты согласия, privacy URL, transport credentials.

**Результат:** PHP уже исполняется на IHC; для production нужны загрузка подтверждённой текущей версии, подтверждение `mail()` и тестовая доставка.

### Этап 13. Cookies, метрики и EnvyBox

**Статус на 20 августа 2026:** cookie consent, отложенная аналитика и Яндекс Метрика `111727875` реализованы локально и опубликованы в Vercel frontend. В Директе сохранена автоматическая цель «заполнил контактные данные», но пользовательские JavaScript-цели ещё не созданы из-за временного `503` интерфейса Метрики. Новый код EnvyBox подключён через consent-aware loader; локальная и публичная Vercel-браузерная проверка показала чат и кнопку обратного звонка. Публикация подтверждённых runtime-файлов на IHC и production event/widget QA остаются открытыми.

**Что делается:**

- cookie banner с Accept/Reject/Settings;
- четыре категории и отдельное хранение версии consent;
- analytics/chat scripts не загружаются до разрешения;
- footer link повторно открывает настройки;
- карта analytics events, включая `application_submitted` после успешной заявки;
- EnvyBox после загрузки нового кода подключает окно чата поддержки, кнопку звонка и форму заказа обратного звонка;
- EnvyBox позиционируется с учетом safe area и системных overlays;
- на mobile виджет не перекрывает меню/форму/cookie UI;
- при отказе от marketing/functional cookies страница остается полностью рабочей.

**Компоненты:** `CookieConsent`, `ConsentPreferencesDialog`, `AnalyticsAdapter`, `ChatAdapter`.

**Данные:** privacy policy, vendor IDs, Envybox settings.

**Результат:** управляемая интеграция внешних скриптов.

### Этап 14. Footer и служебные состояния

**Что делается:**

- телефоны, email, legal links, copyright;
- повторная навигация только по существующим разделам;
- no-JS message формы;
- 404/endpoint error states при необходимости;
- empty/missing image fallbacks;
- reduced motion;
- print-friendly базовый режим.

**Компоненты:** `SiteFooter`, `StatusMessage`.

**Данные:** контакты Екатеринбургского референса и подтвержденные Кировские изменения, если будут.

**Результат:** завершенная страница со всеми системными сценариями.

### Этап 15. QA и приемка

**Функциональная проверка:**

- каждый якорь;
- mobile menu open/close;
- все CTA и передача source/tariff;
- партнерские dialogs;
- topics/program disclosures;
- lightbox и Rutube;
- обе формы success/error;
- consent Accept/Reject/custom categories;
- analytics events;
- Envybox после consent;
- Escape и restore focus.

**Responsive matrix:**

- 320×568;
- 360×800;
- 390/393×844;
- 768×1024;
- 1024×768;
- 1366×768;
- 1440×900;
- 1920×1080.

**Visual QA:**

- баннер показан полностью и без overlays;
- фон не белый и не перегружен;
- карточки согласованы с эскизом, но не копируют его пиксельно;
- нет случайных лиц вместо портретов экспертов;
- нет растянутых логотипов;
- цена, тариф и период визуально связаны;
- modal и cookie UI не конфликтуют с chat widget;
- нет горизонтального overflow.

**Accessibility QA:**

- landmarks и heading order;
- keyboard-only проход;
- visible focus;
- form labels/errors;
- contrast WCAG AA для текста;
- alt texts;
- dialogs и disclosures;
- reduced motion;
- проверка screen reader announcements для формы.

**Performance QA:**

- LCP hero asset;
- отсутствие CLS из-за изображений/виджетов;
- lazy loading ниже fold;
- локальные шрифты или оптимизированная загрузка;
- iframe/video deferred;
- нет неиспользуемых старых assets в production bundle.

**Результат:** QA-report и список закрытых дефектов.

### Этап 16. Публикация

**Статус на 20 августа 2026:** текущая локальная frontend-версия опубликована в Vercel-деплое `dpl_65z5q9bCz1MeURrPnPeL2tGF7BaQ` со статусом `READY` и проверена в публичном браузере; IHC не изменялся. Реальная email-доставка не подтверждена.

**Что делается:**

- проверить целевой домен IHC;
- загрузить PHP endpoint и runtime-файлы малыми порциями;
- проверить запуск PHP без отправки заявки;
- smoke test после отдельного разрешения;
- проверить production domain;
- проверить формы на реальные тестовые адреса с согласованным subject prefix;
- проверить consent и analytics в production;
- сохранить rollback point.

**Компоненты:** deployment config и server-side environment.

**Данные:** домен IHC, runtime-файлы, sender/recipients, analytics IDs.

**Результат:** опубликованный лендинг и проверенная доставка заявок.

## 7. Порядок реализации блоков

Строгий порядок после инфраструктуры:

1. Фон и tokens.
2. Header/navigation.
3. Hero/banner.
4. Партнеры программы.
5. Содержание программы.
6. О проекте.
7. Партнеры проекта + dialog.
8. Программа + CTA.
9. Рестораторы.
10. Ключевые темы + CTA.
11. Стоимость.
12. Что входит.
13. Как проходят мероприятия.
14. Отзывы.
15. Inline form.
16. Footer.
17. Application dialog.
18. Cookie consent.
19. Analytics/Envybox.
20. Responsive/a11y/performance QA.

Такой порядок позволяет сначала закрепить визуальную систему и content flow, затем подключить интерактивность и только после этого внешние интеграции.

## 8. Критерии готовности

Лендинг готов, если одновременно выполнены условия:

- каждый публичный текст имеет подтвержденный источник;
- отсутствующие материалы не заменены выдуманными;
- баннер визуально идентичен утвержденному исходнику;
- дизайн следует эскизу и баннеру, но не копирует Екатеринбург;
- все CTA ведут к работающей форме;
- заявка доставляется на четыре адреса;
- программа не содержит `???`, внутренних комментариев или альтернативных вариантов;
- mobile 320–393 px не имеет горизонтального overflow;
- интерактивность работает клавиатурой;
- consent реально блокирует необязательные скрипты;
- chat widget не перекрывает системные элементы;
- production проходит функциональную, визуальную, accessibility и performance приемку.
